<%= form_with(model: comentario) do |form| %>
  <% if comentario.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(comentario.errors.count, "error") %> prohibited this comentario from being saved:</h2>

      <ul>
        <% comentario.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <div>
      <%= form.label :filme_id, "Escolha um filme" %>
    </div>
    <%= form.collection_select :filme_id, Filme.order(:nome), :id, :titulo %>
  </div>

  <br>

  <div>
    <%= form.label :conteudo,"Escreva seu comentário", style: "display: block"%>
    <%= form.text_area :conteudo, size: "100x10" %>
  </div>

  <br>

  <div>
    <%= form.label :aprovado, style: "display: block" %>
    <%= form.check_box :aprovado %>
  </div>
  
  <br>

  <div>
    <%= form.submit %>
  </div>
<% end %>
